<section class="newsletter-container">
  <div class="heading-container"> 
    <h2><span>New videos,</span> <span>in your inbox,</span> <span>every week.</span></h2>
    <span class="eyes">👀</span>
  </div>
  <form method="post" target="_blank" action="https://tinyletter.com/bholmesdev">
    <input required type="email" name="email" placeholder="whiteboardizen@wtw.dev">
    <button>Subscribe</button>
  </form>
</section>

<style>
  .newsletter-container {
    --anim-duration: 5s;
    
    display: grid;
    background-color: hsl(var(--color-hs), 99%);
    border-radius: 1.8rem;
    margin-block-end: 2rem;
    padding: 1.5rem;
    position: relative;
		display: grid;
		gap: 1rem;
    border: 2px solid var(--color);
    margin-inline: -0.5rem;
	}

  @media (min-width: 700px) {
    .newsletter-container {
      margin-inline: -2rem;
    }
  }

  .newsletter-container::after {
    content: '';
    position: absolute;
    inset: 0;
    bottom: -0.4rem;
    z-index: -1;
    border-radius: inherit;
    background-color: hsl(var(--color-hs), 80%);
    border: 2px solid hsl(var(--color-hs), 80%);
  }

  .heading-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .eyes {
    transform: scale(0);
    transition: transform 0.2s;
  }

	.newsletter-container h2 {
		position: relative;
		margin: 0;
		font-size: var(--font-size-base);
	}

	.newsletter-container h2 > span {
		position: relative;
		z-index: 1;
		animation: newsletter-text-highlight var(--anim-duration) ease-out infinite;
	}

	.newsletter-container h2 > span:nth-child(2) {
		animation-delay: calc(var(--anim-duration) / 3);
	}

	.newsletter-container h2 > span:nth-child(3) {
		animation-delay: calc(var(--anim-duration) / 3 * 2);
	}

	@keyframes newsletter-text-highlight {
		0%, 30%, 100% {
			color: white;
		}
		33%, 98% {
			color: black;
		}
	}

	.newsletter-container h2::before {
		z-index: 0;
		content: '';
		position: absolute;
		top: -0.1em;
		bottom: -0.1em;
		width: 8ch;
		background-color: var(--color);
		transform: scaleX(1.2) translateX(0.1em);
		animation: newsletter-highlight 5s cubic-bezier(0.34, 1.3, 0.64, 1) infinite;
		animation-delay: 0.3s;
		border-radius: 0.2rem;
	}

	@keyframes newsletter-highlight {
		0%, 100%, 25.333% {
			transform: scaleX(1.2) translateX(0.1em);
		}
		33.333%, 58.667% {
			transform: scaleX(1.33) translateX(5em);
		}
		66.667%, 92% {
			transform: scaleX(1.2) translateX(10.8em);
		}
	}

	.newsletter-container form {
		display: flex;
		--radius: 0.4rem;
	}

	.newsletter-container input {
		border: none;
		font-size: var(--font-size-base);
		padding: 0.5rem 1rem;
		background-color: hsl(var(--color-hs), 95%);
		flex: 1;
		border-top-left-radius: var(--radius);
		border-bottom-left-radius: var(--radius);
    font-family: inherit;
	}
	
	.newsletter-container button {
		background-color: var(--color);
		padding: 0.5rem 1rem;
		border-top-right-radius: var(--radius);
		border-bottom-right-radius: var(--radius);
		color: white;
		font-weight: 700;
	}
	
	.newsletter-container :is(button, input):focus {
		outline: 2px solid var(--color);
	}

	.newsletter-container button:is(:focus, :hover) {
    background-color: hsl(var(--color-hs), 70%);
	}

  .newsletter-container input::placeholder {
    color: hsl(var(--color-hs), 80%);
  }

  .newsletter-container:has(input:is(:focus, :hover)) .eyes {
    transform: scale(1.2);
  }

  .newsletter-container:has(button:is(:focus, :hover)) .eyes {
    transform: scale(1.8);
  }

  svg {
    color: hsl(var(--color-hs), 90%);
  }
</style>